apiVersion: v1
kind: Service
metadata:
  namespace: todo-app
  name: frontend-service
  labels:
    app: frontend-service
spec:
  type: NodePort
  ports:
    - name: frontend-service
      port: 80
      targetPort: 80
      nodePort: 31000
  selector:
    app: frontend-pod